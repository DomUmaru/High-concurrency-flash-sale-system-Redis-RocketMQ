<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.cruddemo.mapper.SeckillOrderMapper">

    <!-- 插入订单 -->
    <insert id="insert" parameterType="org.example.cruddemo.entity.SeckillOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO seckill_order (user_id, goods_id, create_time)
        VALUES (#{userId}, #{goodsId}, #{createTime})
    </insert>

    <!-- 防重查询 -->
    <select id="selectByUserIdAndGoodsId" resultType="org.example.cruddemo.entity.SeckillOrder">
        SELECT 
            id, 
            user_id as userId, 
            goods_id as goodsId, 
            create_time as createTime 
        FROM seckill_order 
        WHERE user_id = #{userId} AND goods_id = #{goodsId}
    </select>

</mapper>
